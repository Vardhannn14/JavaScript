// Given the following variables:

// var x = 5, y = 3;

// Calculate and store the result of the following operations in variables:

let x = 5, y = 3

// Addition(+) of `x` and `y`
console.log(a = x + y + "(Addition)"); 

// Subtraction(-) of `x` from `y`
console.log(a = x - y + "(Subtraction)");

// - Multiplication(*) of `x` and `y`
console.log(a = x * y + "(Multiplication)");

// - Division of(/) `x` by `y`
console.log(a = x / y + "(Division)");

