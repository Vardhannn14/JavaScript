// Question 12. Array Methods (push and pop).
// Task: Add an element to the array using `push()` and remove one using `pop()`.
// Rubric: 3 points for correct use of `push()`, 2 points for `pop()`.

let fruit = ["Orange","Pineapple","Apple","Mango","Grapes"];
fruit.push("Kiwi")
console.log("Adding another Fruit= ", fruit);

let removedfruit = fruit.pop();
console.log(removedfruit);